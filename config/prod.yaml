# 生产环境配置（仅用于灰度/线上验证）

# 应用配置
app:
  base_url: "http://example.com"
  env: "prod"

# 账号配置
account:
  username: "prod_user"
  password: "prod_password"

# 浏览器配置（覆盖 base.yaml）
browser:
  headless: true

# 特殊开关
features:
  auto_login: true
  debug_mode: false
