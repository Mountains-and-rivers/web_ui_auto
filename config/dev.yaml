# 开发环境配置

# 应用配置
app:
  base_url: "http://localhost:8080"
  env: "dev"

# 账号配置
account:
  username: "dev_user"
  password: "dev_password"

# 浏览器配置（覆盖 base.yaml）
browser:
  headless: false
  slow_motion: 0

# 特殊开关
features:
  auto_login: true
  debug_mode: true
